package sprites;

/** A Wall sprite. */
public class Wall extends Sprite {

	/**
	 * {@inheritDoc}
	 */
	public Wall(char symbol, int row, int column) {
		super(symbol, row, column);
	}

}
